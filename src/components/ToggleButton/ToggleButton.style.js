import styled from "@emotion/styled";

const Wrapper = styled.button`
  display: flex;
  align-items: center;
  border: none;
  background-color: rgba(0, 0, 0, 0);
`;

const TextWrapper = styled.div`
  flex-shrink: 1;
`;

export default { Wrapper, TextWrapper };
