import styled from "@emotion/styled";
import Common from "styles/common";

// eslint-disable-next-line import/prefer-default-export
export const Option = styled.li`
  &:hover {
    background-color: ${Common.colors.gray06} !important;
  }
`;
